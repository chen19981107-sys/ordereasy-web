services:
  - type: web
    name: ordereasy-api
    env: node
    plan: free
    buildCommand: node build.js
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
    autoDeploy: true
